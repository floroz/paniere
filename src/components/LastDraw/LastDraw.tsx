import LastDrawDisplay from '../LastDrawDisplay';

/**
 * Displays the last drawn number and previous draws
 * Now uses the extracted LastDrawDisplay component for better code organization
 */
const LastDraw = () => {
  return <LastDrawDisplay />;
};

export default LastDraw;
